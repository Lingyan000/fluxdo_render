name: fluxdo_render
description: "fluxdo 自研帖子渲染引擎(Node 模型 + 自研选区 + 虚拟化)。"
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.10.4

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
